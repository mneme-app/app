import { Create } from "./CreatePage";

export const metadata = {
    title: "Mneme · Create",
    description: "Create new notes, quizzes, and flashcards.",
};

export default function CreatePage() {
    return <Create />;
}
